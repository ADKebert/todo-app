require 'test_helper'

class SchedulerTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

  # selects the largest task that fits neatly into a timeslot

  # selects two tasks if more work would be completed in a timeslot than with just one

  # selects a task with an upcoming deadline before a larger task

  

  # reports total time needed to meet deadlines

  # schedules the tasks with the earlier deadlines sooner

  # schedules all tasks before their deadlines
end
